package day07;

//===================클래스의 용도 : 1. 라이브러리 클래스=================
// >>인스턴스화 전에는 메소드 영역에 byte file로 저장되어있음**
//함수 안에서 생성 : 변수 / class 안, 함수 밖에서 생성 : 필드
public class Car {//class start

    //클래스 멤버
    //1. 필드 : 객체의 데이터를 저장하는 공간
        //필드 선언
        //[p.214 : 초기화 없을경우 객체 생성시 기본값으로 초기화]
    String model;
    boolean start;
    int speed;

    //2. 생성자 : 객체 생성 시 초기화

    //3. 메소드 : 객체 동작

}//class end
